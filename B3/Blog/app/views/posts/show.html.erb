
<div style="width:900px; margin:0 auto;">
  <div>
    <p id="notice"><%= notice %></p>
  </div>
  <div>
    <h2><%= @post.title %></h2>
    <p>
      <strong>分类:</strong>
      <%= @post.category %>
      <strong>作者：</strong>
      <%= session[:nickname] %>
      <br>
      <%= image_tag @post.cover_url(:normal), :class => "img-rounded" %>
    </p>    
    <div style="margin-bottom:20px;">
      <%= sanitize(@post.content) %>
    </div>
  </div>
  <div>
    <%= link_to '删除', ".", method: :delete, data: { confirm: '确定要删除这篇文章吗?' },:class => 'btn' %>
    <%= link_to '编辑', edit_admin_post_path,'data-no-turbolink' => true,:class => 'btn'  %> |
    <%= link_to '返回',  posts_path,:class => 'btn' %>
  </div>
  <hr>
  <div>
    <h3 align="center">留言评论</h3>
  </div>
  <div >
    <table class="table">
      <thead>
        <tr>
          <th>Email</th>
          <th>姓名</th>
          <th>留言内容</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @comments.each do |comment| %>
          <% if comment.post_id == @post.id && comment.is_checked  %>
            <tr>
              <td><%= comment.email %></td>
              <td><%= comment.name %></td>
              <td><%= comment.content %></td>
            </tr>
          <% end %>
        <% end %>
      </tbody>
    </table>
  </div>

</div>
