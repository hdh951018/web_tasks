
<!DOCTYPE html>
<html>
<head>
<meta http-equiv="X-UA-Compatible" content="IE=Edge">
<meta charset="utf-8">


<style type="text/css" media="screen">
  .btn.jumbo {
    font-size: 20px;
    font-weight: normal;
    padding: 14px 24px;
    margin-right: 10px;
    -webkit-border-radius: 6px;
    -moz-border-radius: 6px;
    border-radius: 6px;
  }
</style>

<script type="text/javascript">

  var _gaq = _gaq || [];
  _gaq.push(['_setAccount', 'UA-30181385-1']);
  _gaq.push(['_trackPageview']);

  (function() {
    var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
    ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
    var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
  })();

</script>
</head>
<body>

<div class="container">
  <div class="hero-unit" style="margin-top:1px">
    <h4 style="font-size:58px">编辑文章<small>Simple, beautiful  editors</small></h4>
    <hr>
    <%= form_for([:admin,@post],:html => {:multipart => true}) do |f| %>
      <% if @post.errors.any? %>
        <div id="error_explanation">
          <h2>发表失败，有以下 <%= pluralize(@post.errors.count,'') %>个错误：</h2>

          <ul>
          <% @post.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div >
        <%= f.label "文章标题" %> 
        <%= f.text_field :title %>
      </div>
      <div >
        <%= f.label "分类" %> 
        <%= f.select :category, category_list  %>
      </div>
      <div >
        <%= f.label "封面" %> 
        <%= f.file_field :cover %>
        <%= f.hidden_field :cover_cache %>        
      </div>
      <div >
        <%= f.label "正文" %> 
        <%= f.kindeditor :content ,:width => 800, :height => 500 %>
      </div>      
      <div  >
        <%=  submit_tag "发表文章" ,:class => "btn" %>
        <input type="button" value="取消" class="btn" onclick="javascript:location='/admin/posts';">
      </div>
    <% end %>    
  </div>
  
 
</body>
</html>
